# Docker profile: overrides datasource to use the postgres service name
spring.datasource.url=jdbc:postgresql://postgres:5432/timekeeping
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost,http://localhost:80}
